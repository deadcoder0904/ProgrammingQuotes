# Migration `20191010192458-init`

This migration has been generated by Akshay Kadam at 10/10/2019, 7:24:58 PM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "lift"."Quote" (
  "author" TEXT NOT NULL DEFAULT ''  ,
  "content" TEXT NOT NULL DEFAULT ''  ,
  "id" TEXT NOT NULL   ,
  PRIMARY KEY ("id")
);

CREATE UNIQUE INDEX "lift"."Quote.content" ON "Quote"("content")
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration ..20191010192458-init
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,18 @@
+generator photon {
+  provider = "photonjs"
+}
+
+generator nexus_prisma {
+  provider = "nexus-prisma"
+}
+
+datasource db {
+  provider = "sqlite"
+  url      = "file:dev.db"
+}
+
+model Quote {
+  id      String @default(cuid()) @id
+  author  String
+  content String @unique
+}
```

## Photon Usage

You can use a specific Photon built for this migration (20191010192458-init)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20191010192458-init'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
